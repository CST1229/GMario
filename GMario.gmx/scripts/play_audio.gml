///play_audio(sound, priority, loop)
//Used to stop multiple sounds playing at once
//so it doesn't destroy your ears
if (audio_is_playing(argument0)) audio_stop_sound(argument0);
audio_play_sound(argument0,argument1,argument2);
